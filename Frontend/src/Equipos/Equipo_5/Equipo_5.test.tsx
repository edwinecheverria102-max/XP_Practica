import { describe, it } from 'vitest'

describe('Equipo 5 - Perfil de usuario', () => {

  it('debe mostrar la información del usuario (nombre, correo, teléfono, fecha)', () => {})

  it('debe mostrar la lista de libros en posesión con título y fecha de entrega', () => {})

  it('debe mostrar mensaje cuando no hay libros en posesión', () => {})

  it('debe mostrar un botón de editar perfil', () => {})

  it('debe cambiar a modo edición al hacer click en editar', () => {})

  it('debe mostrar inputs con los datos actuales del usuario en modo edición', () => {})

  it('debe permitir actualizar correo y teléfono', () => {})

  it('debe enviar una petición PUT al guardar cambios', () => {})

  it('debe actualizar la vista después de guardar cambios', () => {})

})